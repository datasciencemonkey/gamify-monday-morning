[project]
name = "monday-morning-data"
version = "0.1.0"
description = "Deterministic synthetic data generator for the Monday Morning CPG demo"
requires-python = ">=3.12,<3.13"
dependencies = [
    "databricks-connect>=16.4,<17.4",
    "pandas>=2.0",
    "numpy>=1.26",
]
