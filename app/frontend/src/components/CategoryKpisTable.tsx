// STUB - built live by the component workflow (CategoryKpisTable)
import { Card, Spinner } from './ui'
export default function CategoryKpisTable({ openGenie }: { openGenie: (title: string, prompt: string) => void }) {
  void openGenie
  return <Card><Spinner label="Loading CategoryKpisTable..." /></Card>
}
