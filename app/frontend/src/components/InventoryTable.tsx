// STUB - built live by the component workflow (InventoryTable)
import { Card, Spinner } from './ui'
export default function InventoryTable({ openGenie }: { openGenie: (title: string, prompt: string) => void }) {
  void openGenie
  return <Card><Spinner label="Loading InventoryTable..." /></Card>
}
