// STUB - built live by the component workflow (CategoryDonut)
import { Card, Spinner } from './ui'
export default function CategoryDonut({ openGenie }: { openGenie: (title: string, prompt: string) => void }) {
  void openGenie
  return <Card><Spinner label="Loading CategoryDonut..." /></Card>
}
